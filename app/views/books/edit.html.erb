<h1>Edit Book</h1>
#a way to add logic inside your Rails views to control what content gets rendered based on certain conditions.

#if any errors, like missing title or author, alert is given
<% if @book.errors.any? %>
  <div class="alert alert-danger">
    <h4>There were some errors:</h4>#Default red color for alert in bootstrap
    <ul>#List all the erros in <li> tag
      <% @book.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
    </ul>
  </div>
<% end %>

<%= form_with(model: @book, local: true) do |form| %>
  <div class="form-group">
    <%= form.label :title %>
    <%= form.text_field :title, class: "form-control" %>
  </div> #checks if its editing a new or existing book
  #Style the text input field to be full width

  <div class="form-group">
    <%= form.label :author %>
    <%= form.text_field :author, class: "form-control" %>
  </div>
  #label is generated
  

  <%= form.submit 'Update Book', class: 'btn btn-primary' %>
<% end %>
#Form is submitted
#btn-primary: Default color in bootsrap for the button
