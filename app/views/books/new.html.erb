<h1>New Book</h1>

#Condtionial to update existing book

#Erros method used if erros are submitted
<% if @book.errors.any? %>
  <div class="alert alert-danger">
    <h4>There were some errors:</h4> #Default red color for alerts in bootstrap
    <ul>#Displays the list of errors
      <% @book.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
    </ul>
  </div>
<% end %>

<%= form_with(model: @book, local: true) do |form| %>
  <div class="form-group">
    <%= form.label :title %>
    <%= form.text_field :title, class: "form-control" %>
  </div>#Form is connected to the @book model to update data
   #Style the text input field to be full width

  <div class="form-group">
    <%= form.label :author %>
    <%= form.text_field :author, class: "form-control" %>
  </div>

  <%= form.submit 'Create Book', class: 'btn btn-primary' %>
<% end %> 
#btn-primary: Default color in bootsrap for the button
